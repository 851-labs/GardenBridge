{
  "name": "gardenbridge-mcp",
  "version": "1.0.0",
  "description": "MCP server for GardenBridge - control macOS through Claude",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp src/*.html dist/",
    "dev": "bun run src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.1",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
